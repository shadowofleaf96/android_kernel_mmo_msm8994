# Copy to scripts/remote-cam.env and fill in. Not committed.

REMOTE_HOST=build-host.example
REMOTE_USER=builder

# Same layout as local: $HOME/android/{mmo_msm8994_talkman,out_cam,toolchain}
REMOTE_ANDROID=/home/builder/android

REMOTE_JOBS=8
